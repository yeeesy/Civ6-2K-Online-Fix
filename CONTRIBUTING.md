# 贡献指南

感谢你帮助改进项目。首要原则是：无法证明安全时不写入。

## 开发与验证

需要 Windows 10/11、Visual Studio 2022 C++ x64 工具链、Windows SDK 和 PowerShell。

```powershell
& '.\build.ps1' -Analyze
& '.\build\Civ6_2K_Online_Fix.exe' --self-test
```

提交前应确保警告即错误构建、全部测试和 MSVC `/analyze` 通过。产品版本只修改根目录 `version.json`。

## 修改规则

- 将窗口消息、配置文件、Steam 元数据、路径和远程进程数据视为不可信输入。
- 不得让未知或只读候选构建进入写入路径。
- 不得放宽进程身份、系统运行库、目标所有权或清理生命周期检查。
- 发布和恢复都必须比较当前值、执行回读并分别报告保护与指针结果。
- 行为变化应先有失败测试，再实现修复，并同步更新公开文档。
- 对外诊断只能使用受控字段，不得包含本机路径、PID、内存地址、账号标识或原始平台详情。
- GitHub Actions 保持最小权限，第三方 Action 固定到完整提交。

## 新 Build Profile

请求支持新游戏构建时，请提供官方版本、渠道、EXE 名、大小、SHA-256 和已经脱敏的只读诊断摘要。不要上传游戏 EXE 或原始 JSONL。

新配置必须先保持只读。只有在身份、目标位置、正常流程和负向流程均得到独立验证后，才可考虑启用修复。详细规则见 [`docs/MAINTAINER.md`](docs/MAINTAINER.md)。

## Pull Request

请保持改动聚焦，说明用户可见变化、受影响的安全边界和验证结果。不要提交 `HANDOFF.md`、内部工作资料、个人日志、原始 JSONL、本机 INI、凭据、生成物、签名密钥或版权游戏文件。
