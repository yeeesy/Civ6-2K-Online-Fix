日志为 JSONL，每行一个事件。

默认位置：EXE 旁的 logs 目录；不可写时回退到：
%LOCALAPPDATA%\Civ6_2K_Online_Fix\logs

日志不会主动读取密码或 token，也不会自动发送或上传，但可能包含本地路径、PID、哈希和内存地址。
报告普通问题时优先使用 GUI 的“复制脱敏诊断”。不要直接公开原始 JSONL；确需提供片段时，请先检查并删除本机路径、PID、地址、账号标识和其他无关字段。
