# 安全策略

## 支持范围

当前维护 `1.x` 稳定版本。请先确认问题能够在最新正式版本中复现。

## 安全原则

本工具会在用户明确启动后检查《文明 VI》进程，并仅对源码中标记为受支持的精确游戏构建执行修复。项目长期保持以下边界：

- 无法确认游戏构建、目标进程或系统运行库身份时，不进行写入；
- 不覆盖不属于本工具的进程内改动；
- 无法确认写入、页面保护或恢复结果时，不报告成功；
- 游戏仍可能执行本工具安装的保护代码时，不提前释放相关内存；
- 发布包不包含游戏文件、个人日志、凭据或签名私钥。

这些边界的实现细节以当前源码和自动测试为准。修改相关逻辑时必须同步增加负向测试，不得为了兼容未知构建而放宽检查。

## 报告安全问题

请优先使用本仓库的 private vulnerability reporting 私下报告安全问题。不要在公开 Issue 中发布可利用细节、凭据、账号标识、本机隐私路径、原始日志或完整游戏文件。

报告中建议包含：受影响的工具版本与 SHA-256、Windows 和游戏版本、影响、最小复现步骤，以及已经脱敏的必要诊断信息。

GUI 的“复制脱敏诊断”适合普通公开反馈。`--diagnose` 输出和原始 JSONL 仅保存在本机，但可能含路径、PID、内存地址及文件哈希；发布前必须自行检查并脱敏。

## 不在范围内

- 2K/Steam 服务可用性或账号支持纠纷；
- 已经拥有管理员、内核或任意游戏进程写权限的恶意软件；
- 没有可复现源码缺陷的安全软件启发式告警。
